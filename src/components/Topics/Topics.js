import React from 'react';
import './Topics.css'

const Topics = ({topic}) => {
    // const {name, id, logo} = topic;
    return (
        <div>
            {/* <img src={logo} alt="" /> */}
        </div>
    );
};

export default Topics;